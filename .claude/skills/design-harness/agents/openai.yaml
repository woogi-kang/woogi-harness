interface:
  display_name: "Design Harness"
  short_description: "Opt-in legacy Design Runtime comparison baseline"
  brand_color: "#0F766E"
  default_prompt: "Use $design-harness only because the user explicitly requested the legacy Design Runtime comparison baseline."

policy:
  allow_implicit_invocation: false
