# UI Styling helper test environment
# Python 3.14 recommended by `.claude/registry/tech-stacks/python-fastapi.yaml`.

# Runtime helpers use only the Python standard library.

# Test-only dependencies
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1

# Web tooling is project-owned. When a project selects Tailwind, use the
# recommended web baseline: Node.js 24.18.0 LTS and Tailwind CSS 4.3.2.
# Never install shadcn/Tailwind globally from this requirements file.
